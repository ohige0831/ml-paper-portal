name = "ml-paper-portal-worker"
main = "src/index.ts"
compatibility_date = "2024-10-01"
compatibility_flags = ["nodejs_compat"]

# [TRIAL B案 2026-04-22〜2026-04-29] fetch→summarize を1日3回
# 試験運用終了後は以下に戻すこと:
#   crons = ["0 3 * * *"]
# スケジュール: 09:10 / 15:10 / 21:10 JST (= 00:10 / 06:10 / 12:10 UTC)
# fetch (GitHub Actions) が 00:00 / 06:00 / 12:00 UTC に先行する
[triggers]
crons = ["10 0 * * *", "10 6 * * *", "10 12 * * *"]

[[d1_databases]]
binding = "DB"
database_name = "ml-paper-portal"
database_id = "4c51fd8c-cd8a-4d9b-b2dc-608de38a4c6b"

[vars]
OPENAI_MODEL = "gpt-4o-mini"
SITE_BASE_URL = "https://ml-paper-portal-web.pages.dev"

# Secrets (set via: wrangler secret put OPENAI_API_KEY)
# OPENAI_API_KEY
# ADMIN_PASSWORD
# INGEST_TOKEN    ← GitHub Actions から /api/ingest を叩く際の Bearer token
